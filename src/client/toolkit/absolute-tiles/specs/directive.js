describe('gdToolkitAbsoluteTiles directive', function () {
    beforeEach(function () {
        module('gdToolkitAbsoluteTiles');
        inject(function ($injector) {
            //factory = $injector.get('gdToolkitAbsoluteTilesFactory');
        });
    });
});
